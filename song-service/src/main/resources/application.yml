server:
  port: 8083

spring:
  application:
    name: SONG-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka/}
    instance:
      hostname: localhost